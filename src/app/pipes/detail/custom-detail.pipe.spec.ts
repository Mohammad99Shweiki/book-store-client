import { CustomDetailPipe } from './custom-detail.pipe';

describe('CustomDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
